'use strict';

describe('myApp.block module', function() {

  beforeEach(module('myApp.block'));

  describe('block controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('BlockCtrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});