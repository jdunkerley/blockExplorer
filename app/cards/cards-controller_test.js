'use strict';

describe('myApp.cards module', function() {

  beforeEach(module('myApp.cards'));

  describe('cards controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('CardsCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});